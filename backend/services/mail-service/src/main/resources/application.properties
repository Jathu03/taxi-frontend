# Application Configuration
spring.application.name=mail-service
server.port=8086

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/taxi_system?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=#Mrc25031967
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Mail Configuration (SMTP)
# For Gmail: smtp.gmail.com, port 587
# For other providers, update accordingly
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=s.jathurshan03@gmail.com
spring.mail.password=lqit atfz zjvg pcum
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Logging
logging.level.com.taxi.mail=DEBUG
logging.level.org.springframework.mail=DEBUG
